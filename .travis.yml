language: node_js
language: ruby

services:
  - docker

before_install:
- docker pull node:lts
- docker run -it --rm -v $PWD/pro-vuejs/todo:/todo/ -w /todo/ --user 1000:1000 node:lts
    bash -c "yarn install; yarn build"
script:
- bundle exec rake test