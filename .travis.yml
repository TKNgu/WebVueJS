language: node_js
language: ruby

services:
  - docker

before_install:
- docker pull node:lts
- docker run node:lts /bin/sh -c "cd /root/sinatra; bundle exec foreman start;"
- docker ps -a
- docker run carlad/sinatra /bin/sh -c "cd /root/sinatra; bundle exec rake test"

script:
- bundle exec rake test